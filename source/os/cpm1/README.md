# CP/M 1.x Bring-up

This directory holds CP/M 1.x bring-up notes and build artifacts. No CP/M
binaries are shipped in the repo. Use the loader scripts in
`source/os/common/loader/` with your own CP/M 1.x images.
